<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color (use backgroundTint) -->
    <solid android:color="@android:color/transparent" />

    <!-- Remove the border by omitting the stroke element -->
    <!-- No stroke defined -->

    <!-- Border radius (if needed, set it to 0dp for no rounded corners) -->
    <corners android:radius="0dp" />

    <!-- Padding inside the button -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="5dp"
        android:bottom="5dp" />
</shape>
